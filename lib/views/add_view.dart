import 'package:flutter/material.dart';

// screen to add calories
class AddView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightGreen,
    );
  }
}